
function if_flow:
    trigger:
        print "start if"
        if "hello" is a string:
            print "always met"
        else if false:
            print "never met"
            assert false
        else:
            print "also never met"
            assert false
        print "end"

function while_flow:
    trigger:
        print "start while"
        set {a} to false
        set {b} to true
        while {b}:
            print "blob"
            exit section
            wait 3 seconds
            assert false
            print "boo"
        print "finish while"
        return "ended"

function inline_headers:
    trigger:
        print "1"
        if true:
            assert true
            if false
            assert false
            print "w"
        print "x"
        if false
        print "3"
        print "4"
