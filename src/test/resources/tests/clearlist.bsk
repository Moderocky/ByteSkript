
function test:
    trigger:
        set {var} to a new list
        add 1 to {var}
        assert {var} contains 1
        clear {var}
        assert {var} exists
        assert {var} is a list
        assert ({var} contains 1) is false
        return true
